# Run 2-049 — Riley (P04) on DB Timeout Resilience (J04 under ERR-DB) (RE-TEST)

| Field | Value |
|-------|-------|
| Persona | Riley (04) — 15-company fleet operator |
| Journey | J04 — Daily Triage under ERR-DB condition |
| Original Run | Related to 069, 073 |
| Original Friction | F-069-A (HIGH), F-069-B (CRITICAL), F-073-A (HIGH), F-073-C (CRITICAL) |

## Changes Since Run 1

- **DB query timeout** (`client.ts`): 10-second configurable timeout via `DB_QUERY_TIMEOUT_MS` env var.

## Step-by-Step (against current code)

1. Riley logs in under ERR-DB condition (Turso experiencing elevated latency).
2. Dashboard queries hit the 10-second timeout. `Promise.race` resolves to the timeout rejection.
3. Error propagates — page may show an error or partial render depending on error handling in the route.
4. Key improvement: previously, queries could hang indefinitely (5-10 second blank page, potentially much longer). Now they fail after 10 seconds maximum.
5. Portfolio view: `Promise.all` for Signal computation and pending decisions. If any query times out, the entire Promise.all fails. No partial rendering.
6. Export: synchronous queries with timeout. Large exports may timeout under ERR-DB.

## Friction Events (NEW or RESOLVED)

| ID | Status | Description |
|----|--------|-------------|
| F-069-A | **IMPROVED** | Page fails after 10s max instead of hanging indefinitely. But error state may not be user-friendly |
| F-069-B | **IMPROVED** | Synchronous export timeout prevents infinite hang. But no async fallback |
| F-073-A | **IMPROVED** | 10-second max wait instead of indefinite blank page |
| F-073-C | **IMPROVED** | Export fails fast under ERR-DB. But no retry or async mechanism |
| NEW-2-049-A | NEW (MEDIUM) | Timeout produces an unhandled error — no user-friendly error page or retry suggestion |
| NEW-2-049-B | NEW (MEDIUM) | Portfolio Promise.all fails entirely if any single product query times out — no partial results |

## Verdict: IMPROVED

DB timeout prevents the worst case (indefinite hangs). Pages now fail deterministically after 10 seconds. However, the failure mode is not graceful — no user-friendly error message, no partial results, no retry. This is a reliability improvement, not a full resilience solution.

## Would Recommend: Not Yet
