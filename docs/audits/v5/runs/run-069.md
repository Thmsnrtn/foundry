# Run 069 — Taylor x J09 (Compliance Export)

| Field | Value |
|-------|-------|
| Persona | Taylor (07) — Returning user after 6-week absence |
| Journey | J09 — Compliance Export |
| Fleet | F1 (1 company) |
| Network | NET-AVG (10 Mbps, 80ms latency) |
| Screen | SCR-DESK (1920x1080) |
| A11Y | OFF |
| Account | ACCT-AGED (3+ months) |
| Tier | TIER-GROW (Growth $199) |
| Error | ERR-DB (Turso latency spike 5-10s) |

## Preconditions

Taylor has 1 company on Growth tier. After the 6-week absence, Taylor needs to export a compliance snapshot for the board. The database is experiencing latency spikes (ERR-DB).

## Transcript

### Step 1: Navigate to Privacy & Data under DB latency

Taylor navigates to Privacy & Data. Every page load involves 7+ DB queries via `_shared.ts`.

**Codebase finding:** Under ERR-DB (5-10s per query), the Privacy page load involves `_shared.ts` parallel queries + the Privacy route's own queries (`getOrInitConsents`, `getConsentTimestamps`, `getDataResidencySettings`). The parallel queries in `_shared.ts` should complete in ~5-10s (worst single query time since they're parallel). The Privacy-specific queries (`privacy.ts` line 83-87) add another parallel batch of 3 queries. Total estimated wait: 5-10 seconds.

**Friction:** 5-10 second load time with no progress indication. The server blocks on all queries before sending HTML. No streaming or progressive rendering.

**Result:** FRICTION (HIGH) -- 5-10 second page load under ERR-DB with no loading indication.

### Step 2: Initiate data export

Taylor clicks "Download JSON."

**Codebase finding:** The export handler (`privacy.ts` line 375-402) calls `exportProductData(ctx.productId, 'json')`. This function presumably runs multiple DB queries to gather metrics, briefings, decisions, customers, and agent configuration. Under ERR-DB, each query takes 5-10 seconds.

**Friction:** The export is a synchronous HTTP request. If `exportProductData` makes 5-10 serial DB queries, each taking 5-10 seconds, the total export time could be 25-100 seconds. The browser may show a timeout error. There is no async export with download notification.

**Result:** FRICTION (CRITICAL) -- Synchronous export may timeout under DB latency. No async export mechanism.

### Step 3: Attempt Audit Log for activity history

Taylor navigates to the Audit Log to get agent activity data.

**Codebase finding:** `audit-log.ts` line 79: `const result = await query(sql, params)`. Under ERR-DB, this query takes 5-10 seconds. The page then renders the results (capped at 100 entries).

**Friction:** Combined with the 100-entry cap (F-064-B), the audit log is both slow and incomplete under ERR-DB.

**Result:** FRICTION (HIGH) -- Audit log is slow (5-10s) and incomplete (100-entry cap) under ERR-DB.

### Step 4: Taylor's catch-up context

Taylor returned from a 6-week absence and needs compliance data. The absence means 6 weeks of accumulated agent activity that Taylor has not reviewed.

**Friction:** The compliance export does not include a "summary of changes during period X-Y" format. Taylor cannot get a targeted export for "activity during my 6-week absence" without manually filtering. The audit log's "Since" date filter (line 146-153) helps but is capped at 100 entries.

**Result:** FRICTION (MEDIUM) -- No date-range-specific export. Audit log date filter exists but is capped at 100 entries.

### Step 5: Cross-company boundary check

N/A -- Taylor has 1 company.

**Result:** N/A

## Findings

| ID | Severity | Type | Description |
|----|----------|------|-------------|
| F-069-A | HIGH | reliability | 5-10 second page load under ERR-DB with no loading indication |
| F-069-B | CRITICAL | reliability | Synchronous export may timeout under DB latency |
| F-069-C | HIGH | reliability | Audit log slow and incomplete under ERR-DB |
| F-069-D | MEDIUM | ux | No date-range-specific export for compliance snapshots |

## Cross-Company Boundary

**N/A** -- Single company.
