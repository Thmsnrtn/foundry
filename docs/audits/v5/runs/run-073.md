# Run 073 — Alex x J09 (Compliance Export)

| Field | Value |
|-------|-------|
| Persona | Alex (01) — First-time founder, non-technical |
| Journey | J09 — Compliance Export |
| Fleet | F1 (1 company) |
| Network | NET-AVG (10 Mbps, 80ms latency) |
| Screen | SCR-LAP (1440x900) |
| A11Y | OFF |
| Account | ACCT-EST (2+ weeks) |
| Tier | TIER-SOLO (Solo $79) |
| Error | ERR-DB (Turso latency spike 5-10s) |

## Preconditions

Alex has 1 company on Solo tier. A potential investor has asked for "a data export showing your product's health metrics." Alex is non-technical and unfamiliar with JSON. Database is experiencing latency spikes.

## Transcript

### Step 1: Navigate to Privacy & Data under ERR-DB

Alex navigates to Privacy & Data. Page load is slow due to DB latency.

**Codebase finding:** Under ERR-DB, the Privacy page takes 5-10 seconds to load (7+ parallel queries in `_shared.ts` + 3 Privacy-specific queries in `privacy.ts`).

**Friction:** Alex waits 5-10 seconds with a blank screen. No loading indicator. A non-technical user may think the page is broken and refresh, causing another 5-10 second wait.

**Result:** FRICTION (HIGH) -- 5-10 second blank page under ERR-DB. Non-technical users likely to assume breakage.

### Step 2: Find and click export

Alex scrolls to "Your Data" section and clicks "Download JSON."

**Codebase finding:** The export button is labeled "Download JSON" (privacy.ts line 269). Alex may not know what "JSON" means. The description says "Download a complete copy of your product data in JSON format -- metrics, briefings, decisions, customers, and agent configuration."

**Friction:** For a non-technical founder, "JSON format" is meaningless. Alex needs the data in a format they can understand -- PDF, spreadsheet, or a formatted report. The export is a raw data dump, not an investor-ready document.

**Result:** FRICTION (HIGH) -- JSON export is unusable for non-technical founders. No human-readable export format (PDF, CSV, formatted report).

### Step 3: Export under ERR-DB

Alex clicks "Download JSON." The export handler runs `exportProductData` which makes multiple DB queries under latency.

**Codebase finding:** Under ERR-DB, the export may take 25-100 seconds depending on the number of sequential queries. The browser may show a download spinner or timeout.

**Friction:** Same as F-069-B. Synchronous export may timeout under DB latency. Alex sees a download that never completes.

**Result:** FRICTION (CRITICAL) -- Export likely times out under ERR-DB. Non-technical user has no recourse.

### Step 4: Try to use the exported data

If the export succeeds, Alex opens the JSON file.

**Friction:** JSON is a wall of structured text. Alex cannot present this to an investor. Alex needs a formatted report, dashboard screenshot, or PDF summary. The export is developer-oriented, not founder-oriented.

**Result:** FRICTION (HIGH) -- JSON export is not usable by non-technical founders for investor presentations.

### Step 5: Cross-company boundary check

N/A -- Alex has 1 company.

**Result:** N/A

## Findings

| ID | Severity | Type | Description |
|----|----------|------|-------------|
| F-073-A | HIGH | reliability | 5-10 second blank page under ERR-DB |
| F-073-B | HIGH | ux | JSON export unusable for non-technical founders |
| F-073-C | CRITICAL | reliability | Export may timeout under ERR-DB |
| F-073-D | HIGH | ux | No human-readable export format (PDF, CSV, formatted report) |

## Cross-Company Boundary

**N/A** -- Single company.
