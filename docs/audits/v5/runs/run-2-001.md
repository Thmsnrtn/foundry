# Run 2-001 — Riley (P04) on Fleet Triage Dashboard (J03) (RE-TEST)

| Field | Value |
|-------|-------|
| Persona | Riley (04) — 15-company fleet operator, keyboard-first |
| Journey | J03 — Scaling to 15-Company Fleet |
| Original Run | 057 |
| Original Friction | F-057-A (CRITICAL), F-057-B (CRITICAL), F-057-C (HIGH) |

## Changes Since Run 1

- **Enhanced portfolio view** (`portfolio.ts`): Fleet triage dashboard with Signal scores, risk state badges, pending decision counts, fleet summary bar (avg Signal, red/yellow count, total pending), sorted by urgency (lowest Signal first).
- **Company pause/resume** (`settings.ts`): Paused status shown in portfolio table.

## Step-by-Step (against current code)

1. Riley logs in with 15 companies on Investor-Ready tier.
2. Route hits `GET /portfolio` which requires `multi_product` tier gate. Riley qualifies.
3. `getProductsByOwner` loads all 15 products. `computeSignal` and `getPendingDecisions` run in parallel for all 15 via `Promise.all`.
4. Fleet summary bar renders: avg Signal, red count, yellow count, total pending decisions. Riley sees the fleet health in one glance.
5. Fleet triage table shows all 15 companies sorted by lowest Signal first. Each row shows: company name, Signal score with visual bar, risk badge (RED/YELLOW/GREEN with text labels), pending decision count, status (Active/Paused), and an "Open" button with `aria-label`.
6. Riley can scan the table top-to-bottom: worst companies first. This is O(1) triage, not O(n).
7. Legacy card grid below provides quick-switch cards with Signal scores.

## Friction Events (NEW or RESOLVED)

| ID | Status | Description |
|----|--------|-------------|
| F-057-A | **RESOLVED** | Fleet triage dashboard now exists at `/portfolio` with Signal scores, risk states, pending decisions |
| F-057-B | **IMPROVED** | Pending decision counts visible per-company in fleet table (no unified cross-company decision queue yet) |
| F-057-C | **RESOLVED** | Fleet triage is O(1) for scanning — table sorted by urgency, summary stats at top |
| NEW-2-001-A | NEW (HIGH) | No keyboard shortcuts (j/k) for navigating fleet table rows — Riley's abandonment trigger |
| NEW-2-001-B | NEW (MEDIUM) | No sorting controls — table is always sorted by Signal score, cannot sort by pending decisions or risk |
| NEW-2-001-C | NEW (HIGH) | No batch actions on fleet table (approve all low-risk remediations, dismiss info signals) |

## Verdict: IMPROVED

The fleet triage dashboard is a major improvement. Riley can now see all 15 companies at a glance with health indicators. However, keyboard navigation (j/k), sorting toggles, and batch actions are still absent — which are core to Riley's workflow at 15-company scale.

## Would Recommend: Not Yet
