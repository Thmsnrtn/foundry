# Run 2-020 — Sam (P03) on Compliance Export (J09) (RE-TEST)

| Field | Value |
|-------|-------|
| Persona | Sam (03) — Portfolio operator, 5 companies |
| Journey | J09 — Compliance Export |
| Original Run | Related to 064, 052 |
| Original Friction | F-064-A (CRITICAL), F-052-A (MEDIUM), F-052-C (MEDIUM) |

## Changes Since Run 1

- **Fleet-wide export** (`privacy.ts`): `/settings/export-all` for all products in single file.
- **CSV format** (`privacy.ts`): Both JSON and CSV options.
- **Expanded export** (10 tables): Comprehensive data coverage.
- **Audit log CSV** (`audit-log.ts`): Paginated with bulk export.

## Step-by-Step (against current code)

1. Sam needs a complete data export for an investor due diligence request. Navigates to Privacy & Data.
2. Sam sees "Export All Products (Fleet)" section with JSON and CSV buttons. ARIA label: "Download all products data as JSON."
3. Sam clicks JSON. The handler iterates all 5 products, calls `exportProductData` for each, bundles into a single JSON with `product_count: 5` and per-product sections.
4. File downloads as `foundry-fleet-export-YYYY-MM-DD.json`. Contains: exported_at, founder_id, products array with 10 data categories each.
5. Sam also exports audit log via Audit Log > Export CSV. Gets full activity history without the old 100-entry cap.
6. Sam verifies: export covers metrics, briefings, decisions, customers, agent_config, stressors, audit_scores, lifecycle_state, competitors, competitive_signals for each of 5 products.

## Friction Events (NEW or RESOLVED)

| ID | Status | Description |
|----|--------|-------------|
| F-064-A | **RESOLVED** | Fleet-wide export in single file |
| F-052-A | **RESOLVED** | CSV format available |
| F-052-C | **RESOLVED** | Fleet-wide bulk export — no per-company repetition |
| NEW-2-020-A | NEW (MEDIUM) | Export is synchronous — 5 products with months of data may take significant time |
| NEW-2-020-B | NEW (LOW) | No progress indicator during export download |

## Verdict: SUCCESS

Sam's investor due diligence export workflow is complete. Single-file fleet export in JSON or CSV, expanded data coverage, and audit log bulk export. The synchronous nature may be slow for large datasets but is functional.

## Would Recommend: Yes
