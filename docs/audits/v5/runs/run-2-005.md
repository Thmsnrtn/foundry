# Run 2-005 — Devon (P09) on Settings & Billing (J08) (RE-TEST)

| Field | Value |
|-------|-------|
| Persona | Devon (09) — Screen reader + keyboard user, 3 companies |
| Journey | J08 — Settings & Billing Multi-Company |
| Original Run | 059 |
| Original Friction | F-059-A (CRITICAL), F-059-B (CRITICAL), F-059-C (CRITICAL), F-059-D (HIGH), F-059-E (MEDIUM) |

## Changes Since Run 1

- **ARIA labels** (`layout.ts`): Product switcher `<select>` now has `aria-label="Switch company"`. Notification bell `<summary>` has `aria-label="Notifications"`. Sidebar `<nav>` has `aria-label="Main navigation"`.
- **Delete modal ARIA** (`privacy.ts`): Modal now has `role="dialog"`, `aria-modal="true"`, `aria-label="Confirm product deletion"`. Escape key closes modal.

## Step-by-Step (against current code)

1. Devon loads dashboard with VoiceOver. Skip link "Skip to main content" is present. `<html lang="en">` set.
2. Devon tabs to header. Product switcher `<select>` announces "Switch company, popup button" — `aria-label="Switch company"` is read. Devon can identify and use the control.
3. Explicit "Go" button (`layout.ts:186`) means no auto-submit on select change. Devon can browse options safely with arrow keys, then click "Go" to submit. This resolves the F-060-E accidental submission issue.
4. Devon tabs to notification bell. `<summary aria-label="Notifications">` — VoiceOver announces "Notifications, disclosure triangle." Functional.
5. Devon tabs through sidebar. `<nav aria-label="Main navigation">` — VoiceOver announces "Main navigation, navigation." Distinguishable from mobile bottom nav which also has `aria-label="Main navigation"`.
6. Devon navigates to Privacy & Data. Delete button has `aria-label="Delete current product data"`. Modal opens with proper `role="dialog"` and focus context.
7. Escape key closes the delete modal (script in privacy.ts handles keydown).

## Friction Events (NEW or RESOLVED)

| ID | Status | Description |
|----|--------|-------------|
| F-059-A | **RESOLVED** | Product switcher has `aria-label="Switch company"` |
| F-059-B | **RESOLVED** | Notification bell has `aria-label="Notifications"` |
| F-059-C | **RESOLVED** | Delete modal has `role="dialog"`, `aria-modal="true"`, `aria-label`, Escape-to-close |
| F-059-D | **RESOLVED** | Sidebar nav has `aria-label="Main navigation"` |
| F-059-E | **STILL OPEN** | Sidebar sections still inconsistent — AGENTS uses details/summary, others use plain div |
| F-060-E | **RESOLVED** | Product switcher now has explicit "Go" button — no auto-submit on change |
| NEW-2-005-A | NEW (MEDIUM) | Both sidebar nav and mobile bottom nav have identical `aria-label="Main navigation"` — should be distinct |
| NEW-2-005-B | NEW (MEDIUM) | Delete modal does not trap focus — Devon can Tab out of the modal into background content |

## Verdict: IMPROVED

Major accessibility improvements. All three CRITICAL ARIA issues from Run 1 are resolved. Devon can now identify and use the product switcher, notification bell, and delete modal via screen reader. Focus trapping in the modal remains incomplete.

## Would Recommend: Not Yet
