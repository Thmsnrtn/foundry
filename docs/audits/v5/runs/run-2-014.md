# Run 2-014 — Devon (P09) on Daily Triage (J04) (RE-TEST)

| Field | Value |
|-------|-------|
| Persona | Devon (09) — Screen reader user, 3 companies |
| Journey | J04 — Daily Founder Triage |
| Original Run | Related to 059, 060 |
| Original Friction | F-059-A (CRITICAL), F-060-E (CRITICAL), F-060-C (HIGH) |

## Changes Since Run 1

- **ARIA on product switcher** (`layout.ts`): `aria-label="Switch company"` on select.
- **Explicit Go button** (`layout.ts`): Product switcher now has "Go" submit button — no auto-submit on change.
- **ARIA on sidebar** (`layout.ts`): `aria-label="Main navigation"` on sidebar nav.

## Step-by-Step (against current code)

1. Devon logs in. VoiceOver announces page title. Skip link works.
2. Devon uses product switcher to check each company. `aria-label="Switch company"` — VoiceOver reads "Switch company, popup button." Devon uses arrow keys to browse, then activates "Go" button to switch.
3. No accidental product switch during keyboard navigation — "Go" button requires explicit activation.
4. Devon navigates to `/portfolio` (fleet triage). Table renders with proper `<table>` structure. `<th>` headers announce column names. Each row's "Open" button has `aria-label="Open dashboard for [CompanyName]"`.
5. Devon can Tab through the table and hear company names, Signal scores (as text), risk labels (as text, e.g., "RED — Recovery"), and pending counts.
6. Risk state is conveyed via text label, not just color: "RED — Recovery", "YELLOW — Monitoring", "GREEN — Stable". Devon can distinguish risk states.

## Friction Events (NEW or RESOLVED)

| ID | Status | Description |
|----|--------|-------------|
| F-059-A | **RESOLVED** | Product switcher accessible with label |
| F-060-E | **RESOLVED** | No auto-submit — explicit "Go" button prevents accidental switches |
| F-060-C | **RESOLVED** | Fleet-level view accessible — portfolio table is semantic HTML with ARIA labels |
| NEW-2-014-A | NEW (MEDIUM) | Signal score bar (visual progress indicator) has no text alternative in portfolio table — only the number is readable |
| NEW-2-014-B | NEW (LOW) | Portfolio summary cards (avg Signal, red count, etc.) are not in a description list — just visual cards |

## Verdict: SUCCESS

Devon can perform daily triage across 3 companies using screen reader. Product switcher is accessible, portfolio table is semantic, risk states are text-labeled. Core accessibility barriers are removed.

## Would Recommend: Yes
