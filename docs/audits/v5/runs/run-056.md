# Run 056 — Jamie x J05 (Remediation PR Review)

| Field | Value |
|-------|-------|
| Persona | Jamie (02) — Serial founder, multi-product operator |
| Journey | J05 — Remediation Engine PR Review |
| Fleet | F2 (2-3 companies) |
| Network | NET-AVG (10 Mbps, 80ms latency) |
| Screen | SCR-LAP (1440x900) |
| A11Y | OFF |
| Account | ACCT-EST (2+ weeks) |
| Tier | TIER-GROW (Growth $199) |
| Error | ERR-NONE |

## Preconditions

Jamie has 2 companies on Growth tier. One company has a pending AUTO-classified remediation PR generated by Atlas (CTO agent) for a security issue found during audit.

## Transcript

### Step 1: Receive notification about pending remediation

Jamie logs in and sees a notification in the notification bell.

**Codebase finding:** The notification bell in layout.ts line 192-215 uses `<details>/<summary>` pattern. The bell emoji (line 196) has no `aria-label` on the `<summary>` element. Notifications include `action_url` which should link to the remediation detail. The `navBadges` in `_shared.ts` line 142 tracks `open_prs_count`.

**Result:** PASS -- Notification appears for pending remediation.

### Step 2: Navigate to remediation detail

Jamie clicks through the notification to view the remediation.

**Codebase finding:** `src/routes/dashboard/agents-remediations.ts` handles the remediation queue. The route loads pending remediations via `getPendingRemediations()` from `src/services/scp/remediation.ts`. Each remediation shows severity badge, agent badge, type label, and description.

**Result:** PASS -- Remediation detail view loads with context.

### Step 3: Review what changed and why

Jamie reviews the remediation: what was found, which audit dimension it addresses, risk assessment, and the proposed fix.

**Codebase finding:** The remediation item display (agents-remediations.ts lines 47-56) shows severity (critical/high/medium/low), agent name (atlas/crucible/sentinel), and remediation type (code_quality, security, test_coverage, infra, performance, compliance). The item includes description text. However, it is unclear whether the PR diff or GitHub link is shown inline.

**Friction:** The remediation view shows metadata and description, but the actual code changes require clicking through to GitHub. There is no inline diff viewer in Foundry. Jamie must leave Foundry to review the actual PR.

**Result:** FRICTION (MEDIUM) -- No inline diff review. Must click through to GitHub for code-level review.

### Step 4: Approve or reject the remediation

Jamie wants to approve the remediation from within Foundry.

**Codebase finding:** `agents-remediations.ts` has POST routes for `/agents/remediations/:id/resolve` and `/agents/remediations/:id/dismiss`. These mark the remediation as resolved or dismissed. However, these actions update the Foundry-side status -- it is unclear whether they trigger a GitHub PR merge.

**Friction:** The "resolve" action in Foundry may not actually merge the PR on GitHub. The journey expects "Approves within Foundry -> PR merged" but the codebase shows only status updates, not GitHub API merge calls. Jamie may need to merge on GitHub separately and then mark as resolved in Foundry -- a two-step process.

**Result:** FRICTION (HIGH) -- Resolve/dismiss in Foundry does not appear to trigger GitHub PR merge. Two-step approval process breaks the "2 clicks maximum" success criterion.

### Step 5: Verify next audit reflects the fix

After merging (on GitHub) and resolving (in Foundry), Jamie expects the next audit cycle to confirm the issue is resolved.

**Codebase finding:** The audit pipeline in `src/services/audit/` runs as a scheduled job. The next audit cycle would re-analyze the repo. Since audits are periodic (not triggered on merge), there may be a delay before the fix is reflected in scores.

**Result:** PASS (deferred) -- Audit will eventually reflect the fix, but not immediately.

### Step 6: Cross-company remediation triage

Jamie wants to check if the other company also has pending remediations.

**Codebase finding:** The remediation queue is per-product (scoped by `productId`). There is no cross-company remediation queue. Jamie must switch products and navigate to the remediation page again.

**Friction:** No fleet-level remediation triage. At 2 companies this is manageable (2 switches), but it violates the J05 fleet-size relevance criterion: "A founder with 15 companies should not need to visit 15 separate remediation queues."

**Result:** FRICTION (MEDIUM) -- No cross-company remediation queue. Must visit each company individually.

### Step 7: Cross-company boundary check

Jamie switches to the other company. The remediation queue shows only that company's items.

**Cross-company awareness check:** PASS. Remediation data is correctly scoped to the selected product.

**Result:** PASS -- Boundary maintained.

## Findings

| ID | Severity | Type | Description |
|----|----------|------|-------------|
| F-056-A | MEDIUM | ux | No inline diff review; must leave Foundry for GitHub |
| F-056-B | HIGH | workflow | Resolve/dismiss does not trigger GitHub PR merge |
| F-056-C | MEDIUM | fleet-scale | No cross-company remediation queue |

## Cross-Company Boundary

**PASS** -- Remediation data correctly scoped per product. No leakage between companies.
