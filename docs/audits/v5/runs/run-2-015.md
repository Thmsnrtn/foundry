# Run 2-015 — Devon (P09) on Compliance Export (J09) (RE-TEST)

| Field | Value |
|-------|-------|
| Persona | Devon (09) — Screen reader user, 3 companies |
| Journey | J09 — Compliance Export |
| Original Run | Related to 059, 064 |
| Original Friction | F-059-A (CRITICAL), F-064-A (CRITICAL) |

## Changes Since Run 1

- **ARIA labels on export buttons** (`privacy.ts`): Each export button has descriptive `aria-label`.
- **Fleet-wide export** (`privacy.ts`): "Export All Products" with JSON and CSV options.
- **CSV format** (`privacy.ts`): `?format=csv` download option.

## Step-by-Step (against current code)

1. Devon navigates to Privacy & Data. Screen reader reads section headings in order.
2. Section 3 "Your Data": VoiceOver reads "Export Current Product" heading, then buttons: "Download current product data as JSON, link" and "Download current product data as CSV, link".
3. Devon activates JSON link — file downloads. Devon activates CSV link — file downloads.
4. For fleet export: "Export All Products (Fleet)" heading, then "Download all products data as JSON, link" and "Download all products data as CSV, link".
5. Devon activates fleet CSV export — single file covering all 3 products downloads.
6. Audit log page: Devon navigates to Audit Log. Filter controls have labels. "Export CSV" button has `aria-label="Export audit log as CSV"`.

## Friction Events (NEW or RESOLVED)

| ID | Status | Description |
|----|--------|-------------|
| F-059-A | **RESOLVED** | All interactive elements have ARIA labels |
| F-064-A | **RESOLVED** | Fleet-wide export accessible via labeled links |
| NEW-2-015-A | NEW (LOW) | Date input for audit log filter may have limited screen reader compatibility depending on browser |

## Verdict: SUCCESS

Devon can complete the entire compliance export workflow via screen reader. All buttons are labeled, links are descriptive, and fleet-wide export is available. No blocking accessibility issues.

## Would Recommend: Yes
