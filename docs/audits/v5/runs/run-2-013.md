# Run 2-013 — Devon (P09) on Full Org Deletion (J10) (RE-TEST)

| Field | Value |
|-------|-------|
| Persona | Devon (09) — Screen reader user, 3 companies |
| Journey | J10 — Full Org Deletion |
| Original Run | Related to 059, 063 |
| Original Friction | F-059-C (CRITICAL), F-063-A (CRITICAL) |

## Changes Since Run 1

- **ARIA on delete modal** (`privacy.ts`): `role="dialog"`, `aria-modal="true"`, `aria-label="Confirm product deletion"`, Escape-to-close.
- **Fleet-wide deletion** (`privacy.ts`): "Delete All Products" confirmation page listing all products.
- **ARIA labels on buttons** throughout privacy.ts: export and delete buttons have `aria-label`.

## Step-by-Step (against current code)

1. Devon navigates to Privacy & Data using keyboard. Page loads with proper heading hierarchy.
2. Section 3 "Your Data" has buttons with `aria-label`: "Download current product data as JSON", "Download current product data as CSV", "Delete current product data", "Delete all products".
3. Devon activates "Delete all products" link — navigates to `/settings/delete-all-products` confirmation page.
4. Confirmation page lists all 3 products by name and ID. "Cancel" and "Yes, Delete All 3 Products" buttons present.
5. Devon can Tab through the product list and action buttons. Screen reader announces product names.
6. For single-product deletion: Devon activates "Delete This Product". Modal opens with `role="dialog"` — VoiceOver announces "Confirm product deletion, dialog".
7. Modal content is readable. "Cancel" and "Yes, Delete [ProductName]" buttons are Tab-accessible. Escape closes modal.

## Friction Events (NEW or RESOLVED)

| ID | Status | Description |
|----|--------|-------------|
| F-059-C | **RESOLVED** | Delete modal has proper ARIA roles, label, and Escape-to-close |
| F-063-A | **RESOLVED** | Fleet-wide deletion available — no need for O(n) individual deletions |
| NEW-2-013-A | NEW (MEDIUM) | Delete modal does not trap focus — Devon can Tab past modal into background elements |
| NEW-2-013-B | NEW (LOW) | Fleet deletion confirmation page does not use `role="alertdialog"` for the destructive action |

## Verdict: IMPROVED

Devon can now complete the full deletion flow via screen reader. ARIA roles on the modal are correct. Fleet-wide deletion eliminates the O(n) problem. Focus trapping remains the main gap.

## Would Recommend: Not Yet
