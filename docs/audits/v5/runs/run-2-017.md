# Run 2-017 — Riley (P04) on Full Org Deletion (J10) (RE-TEST)

| Field | Value |
|-------|-------|
| Persona | Riley (04) — 15-company fleet operator |
| Journey | J10 — Full Org Deletion |
| Original Run | 063 |
| Original Friction | F-063-A (CRITICAL), F-063-B (HIGH), F-063-C (HIGH), F-063-D (HIGH) |

## Changes Since Run 1

- **Fleet-wide deletion** (`privacy.ts`): "Delete All Products" with confirmation page enumerating every product.
- **Fleet-wide export** (`privacy.ts`): "Export All Products" in JSON/CSV before deletion.
- **Data deletion executor** (`jobs/index.ts`): Wired into job scheduler, runs daily.

## Step-by-Step (against current code)

1. Riley navigates to Privacy & Data. Section shows "Delete All Products (Fleet)" button since Riley has 12+ companies.
2. Riley clicks "Delete All Products" — redirected to `/settings/delete-all-products` confirmation page.
3. Confirmation page shows: heading "Delete All Products", warning about 30-day grace period, and a list of all 12 products by name and ID with red bullet indicators.
4. Page offers "Export all data" link before proceeding. Riley clicks it first — downloads fleet-wide JSON export.
5. Riley clicks "Yes, Delete All 12 Products". POST handler iterates through all products calling `scheduleDataDeletion` for each.
6. Redirect to `/privacy?success=all_deletion_scheduled`. Banner: "Deletion scheduled for all 12 products. Data will be removed after 30 days."
7. Deletion executor runs daily at 3 UTC. After 30 days, data is hard-deleted across 14 tables for each product, GitHub tokens nulled.

## Friction Events (NEW or RESOLVED)

| ID | Status | Description |
|----|--------|-------------|
| F-063-A | **RESOLVED** | Fleet-wide deletion in single action — no more O(n) serial deletion |
| F-063-B | **RESOLVED** | Fleet-wide export available before deletion |
| F-063-C | **STILL OPEN** | Products still appear in navigation during 30-day deletion window (no 'deletion_scheduled' status filter) |
| F-063-D | **STILL OPEN** | No billing cancellation prompt — Riley must cancel Stripe separately |
| F-065-B | **STILL OPEN** | GitHub token set to NULL but not revoked via GitHub API |

## Verdict: IMPROVED

Fleet-wide deletion is a major improvement — single action instead of 48+ interactions. Export-before-delete workflow is supported. Remaining gaps: zombie products in nav during grace period, no auto-billing cancellation, no GitHub token revocation.

## Would Recommend: Not Yet
